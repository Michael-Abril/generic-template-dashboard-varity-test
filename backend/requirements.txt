# Varity Generic Template - Backend Requirements
# Decentralized storage with Filecoin/IPFS + Lit Protocol encryption

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.2.0  # Required for Pydantic EmailStr validation

# HTTP Client (for Pinata API)
httpx==0.25.2

# Environment Management
python-dotenv==1.0.0

# Database (PostgreSQL + Redis + SQLite)
sqlalchemy==2.0.23
asyncpg==0.29.0  # PostgreSQL async driver
psycopg2-binary==2.9.9  # PostgreSQL sync driver (required for Alembic migrations)
aiosqlite==0.19.0  # SQLite async driver (for development)
redis==4.6.0

# Database Migrations
alembic==1.13.1  # Database migrations

# Web3 & Blockchain - Compatible versions for Arbitrum
web3==6.11.3
eth-utils>=4.0.0  # Required for L3 commitment service (keccak hashing)
eth-account>=0.11.0  # Required for wallet operations

# Logging & Monitoring
python-json-logger==2.0.7

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Load Testing & Performance
locust==2.17.0  # Load testing framework

# Development
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# CORS support
python-multipart==0.0.6

# Multi-Tenant RAG Infrastructure
qdrant-client>=1.12.0  # Vector database for RAG (updated for server compatibility)
numpy==1.24.3  # For data processing
# Using Ollama embeddings instead of sentence-transformers to avoid PyTorch dependency

# Email Service
sendgrid==6.10.0  # Email notifications and reports
jinja2==3.1.2  # Email templates

# PDF Generation & Extraction
reportlab==4.0.7  # PDF report generation
matplotlib==3.7.5  # Charts for reports (3.7.5 max for Python 3.8)
pillow==10.1.0  # Image processing for PDFs
PyMuPDF==1.23.8  # PDF text extraction (fitz)
python-docx==1.1.0  # Word document extraction

# Excel Export
openpyxl==3.1.2  # Excel file generation for data export

# OAuth & Authentication
authlib==1.3.0  # OAuth 2.0 client for all integrations
cryptography==46.0.3  # Required for authlib

# Background Jobs
celery[redis]==5.3.4  # Task queue for scheduled syncs
flower==2.0.1  # Celery monitoring (optional)

# Real Integration SDKs
python-quickbooks==0.9.12  # QuickBooks API (latest available version)
stripe==7.8.0  # Stripe payments
google-auth==2.25.2  # Google OAuth
google-api-python-client==2.111.0  # Google Workspace APIs
simple-salesforce==1.12.5  # Salesforce API
# Note: Shopify SDK will be implemented using direct API calls
slack-sdk==3.26.1  # Slack workspace
hubspot-api-client==8.2.1  # HubSpot CRM
zenpy==2.0.46  # Zendesk support

# Additional utilities
python-dateutil==2.8.2  # Date parsing for integrations
pytz==2023.3  # Timezone handling
# Note: cryptography==46.0.3 already specified above (line 61)

# Optional: Lit Protocol Integration
# Note: No official Python SDK yet. Using direct HTTP API calls

# Note: Monday.com doesn't have official Python SDK, using GraphQL via httpx
