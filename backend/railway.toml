# Railway Configuration for Varity Backend
# This file tells Railway how to build and deploy the FastAPI backend

[build]
builder = "nixpacks"

[deploy]
startCommand = "bash start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
